name: Autonomous Upgrade Canary

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  # ── Check: read-only version discovery ──────────────────────────────────
  check-new-version:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      latest-lc4j: ${{ steps.lc4j.outputs.latest }}
      current-lc4j: ${{ steps.lc4j.outputs.current }}
      should-upgrade-lc4j: ${{ steps.lc4j.outputs.should-upgrade }}
      latest-boot: ${{ steps.boot.outputs.latest }}
      current-boot: ${{ steps.boot.outputs.current }}
      should-upgrade-boot: ${{ steps.boot.outputs.should-upgrade }}
    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      - name: Check LangChain4j version
        id: lc4j
        run: |
          set -euo pipefail
          RAW_RELEASE=$(curl -sf "https://repo1.maven.org/maven2/dev/langchain4j/langchain4j-bom/maven-metadata.xml" \
            | grep -oP '<release>\K[^<]+' | head -1)
          LATEST="$RAW_RELEASE"
          if [ -z "$LATEST" ]; then
            echo "Failed to fetch latest LangChain4j version" >&2
            exit 1
          fi
          CURRENT=$(grep -oP '<langchain4j\.version>\K[^<]+' pom.xml)
          echo "latest=$LATEST" >> "$GITHUB_OUTPUT"
          echo "current=$CURRENT" >> "$GITHUB_OUTPUT"
          echo "should-upgrade=$([ "$LATEST" != "$CURRENT" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "LangChain4j: $CURRENT → $LATEST"

      - name: Check Spring Boot version
        id: boot
        run: |
          set -euo pipefail
          LATEST=$(curl -sf "https://repo1.maven.org/maven2/org/springframework/boot/spring-boot-dependencies/maven-metadata.xml" \
            | grep -oP '<release>\K[^<]+' | head -1)
          if [ -z "$LATEST" ]; then
            echo "Failed to fetch latest Spring Boot version" >&2
            exit 1
          fi
          CURRENT=$(grep -oP '<spring-boot\.version>\K[^<]+' pom.xml)
          echo "latest=$LATEST" >> "$GITHUB_OUTPUT"
          echo "current=$CURRENT" >> "$GITHUB_OUTPUT"
          echo "should-upgrade=$([ "$LATEST" != "$CURRENT" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "Spring Boot: $CURRENT → $LATEST"

  # ── Upgrade: write operations (only if check job finds an upgrade) ──────
  upgrade-new-version:
    runs-on: ubuntu-latest
    needs: check-new-version
    if: ${{ needs.check-new-version.outputs.should-upgrade-lc4j == 'true' || needs.check-new-version.outputs.should-upgrade-boot == 'true' }}
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      - name: Apply LangChain4j upgrade
        if: needs.check-new-version.outputs.should-upgrade-lc4j == 'true'
        run: |
          mvn versions:set-property \
            -Dproperty=langchain4j.version \
            -DnewVersion=${{ needs.check-new-version.outputs.latest-lc4j }} -q
          echo "Updated langchain4j.version to ${{ needs.check-new-version.outputs.latest-lc4j }}"

      - name: Apply Spring Boot upgrade
        if: needs.check-new-version.outputs.should-upgrade-boot == 'true'
        run: |
          mvn versions:set-property \
            -Dproperty=spring-boot.version \
            -DnewVersion=${{ needs.check-new-version.outputs.latest-boot }} -q
          echo "Updated spring-boot.version to ${{ needs.check-new-version.outputs.latest-boot }}"

      - name: Commit version bumps
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pom.xml
          git commit -m "chore: bump dependencies" || echo "No changes to commit"

      - name: Run Canary Evals (upgrade candidate)
        run: mvn test

      - name: Create or find Pull Request
        id: create-pr
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BRANCH="auto-upgrade/lc4j-${{ needs.check-new-version.outputs.latest-lc4j }}-boot-${{ needs.check-new-version.outputs.latest-boot }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git fetch origin
          git push origin "$BRANCH" --force-with-lease

          BODY=""
          if [ "${{ needs.check-new-version.outputs.should-upgrade-lc4j }}" == "true" ]; then
            BODY="${BODY}LangChain4j ${{ needs.check-new-version.outputs.current-lc4j }} → ${{ needs.check-new-version.outputs.latest-lc4j }}"
          fi
          if [ "${{ needs.check-new-version.outputs.should-upgrade-boot }}" == "true" ]; then
            [ -n "$BODY" ] && BODY="${BODY}$(printf '\n')Spring Boot ${{ needs.check-new-version.outputs.current-boot }} → ${{ needs.check-new-version.outputs.latest-boot }}"
          fi
          BODY="${BODY}$(printf '\n\nAll canary evals passed with the new version.')"

          TITLE=""
          if [ "${{ needs.check-new-version.outputs.should-upgrade-lc4j }}" == "true" ] && \
             [ "${{ needs.check-new-version.outputs.should-upgrade-boot }}" == "true" ]; then
            TITLE="Upgrade LangChain4j and Spring Boot"
          elif [ "${{ needs.check-new-version.outputs.should-upgrade-lc4j }}" == "true" ]; then
            TITLE="Upgrade LangChain4j to ${{ needs.check-new-version.outputs.latest-lc4j }}"
          else
            TITLE="Upgrade Spring Boot to ${{ needs.check-new-version.outputs.latest-boot }}"
          fi

          EXISTING=$(gh pr list --state open --head "$BRANCH" --json url --jq '.[0].url' || true)
          if [ -n "$EXISTING" ]; then
            echo "PR already exists: $EXISTING"
            echo "url=$EXISTING" >> "$GITHUB_OUTPUT"
          else
            gh pr create \
              --title "$TITLE" \
              --body "$BODY" \
              --label dependencies \
              --reviewer ${{ github.repository_owner }}
            echo "PR created"
          fi

      - name: Report result
        run: |
          if [ "${{ needs.check-new-version.outputs.should-upgrade-lc4j }}" == "true" ]; then
            echo "LangChain4j: ${{ needs.check-new-version.outputs.current-lc4j }} → ${{ needs.check-new-version.outputs.latest-lc4j }}"
          fi
          if [ "${{ needs.check-new-version.outputs.should-upgrade-boot }}" == "true" ]; then
            echo "Spring Boot: ${{ needs.check-new-version.outputs.current-boot }} → ${{ needs.check-new-version.outputs.latest-boot }}"
          fi

  # ── Canary test: pinned version (always runs) ──────────────────────────
  canary-test-current:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: temurin
          cache: maven
      - name: Run Canary Evals (pinned version)
        run: mvn test
